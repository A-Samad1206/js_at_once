// Promise.all
/*
Static method take array of promise as input, and 
if all input promised reolves return a promise that resolve to an array of 
results of input promises.
if any of input promise rejected, quickly return promise that 
rejected with first reason. and ignore other promises whether they will be resolved or rejected.
*/
